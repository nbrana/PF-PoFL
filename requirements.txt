# For lighter CPU-only PyTorch: pip install torch --index-url https://download.pytorch.org/whl/cpu
# then install remaining lines, or use: pip install -r requirements.txt after editing torch source.
numpy>=1.24
scipy>=1.11
torch>=2.0
pytest>=7.0
